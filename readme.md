How to run
==========

*if not instaled
  *install node js
  *install gulp and bower (npm install -g gulp, npm install -g bower)
  
*clone the repo

*run npm install

*run bower install

*run gulp watch
